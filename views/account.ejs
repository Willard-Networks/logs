<!DOCTYPE html>
<html lang="en">

<head>
  <%- include("partials/head") %>
</head>

<body>
  <%- include("partials/header") %>
  <div class="container pl-sm-0 pr-sm-0 pl-lg-3 pr-lg-3">
    <h2><img src="<%= user.photos && user.photos[2] ? user.photos[2].value : 
                   user.photos && user.photos[1] ? user.photos[1].value :
                   user.photos && user.photos[0] ? user.photos[0].value :
                   user.avatar %>" 
             alt="Your Avatar Image" /></h2>
    <h2>ID: <%= user.id %></h2>
    <h2>Name: <%= user.displayName %></h2>
    <h2>Rank: <%= rank %></h2>
  </div>
  <%- include("partials/footer") %> <%- include("partials/scripts") %>
</body>

</html>
